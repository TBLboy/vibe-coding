---
description: Verify acceptance criteria and record Vibe Coding evidence
agent: vibe-main
---

使用 `a-verification` 对照 `done_when` 和验收标准验证当前实现。优先使用独立 `verification-reviewer`；高风险任务必须记录覆盖产物的有效证据和独立复核结论。失败时定位最早需要回退的生命周期阶段。

用户请求：$ARGUMENTS
