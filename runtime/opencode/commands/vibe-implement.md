---
description: Specify and implement an approved Vibe Coding task
agent: vibe-main
---

确认任务、业务逻辑、决策和写入边界均已明确后，使用 `a-engineering-spec` 补足工程说明，再由 `implementation-builder` 或主 Agent 执行最小一致改动。记录改动文件、命令和验证结果；未独立验证时只能报告为 `implemented-unverified`。

用户请求：$ARGUMENTS
