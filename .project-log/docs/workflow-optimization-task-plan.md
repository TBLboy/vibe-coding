# 四项框架优化任务计划

日期：2026-09-19。规划任务：TASK-013。前置研究：TASK-012 / RES-001。方向决定：DEC-006。

## 当前状态与授权边界

- 用户已同意以本机 SQLite 管理机器状态、Git 保存可读交换快照的方向，先通过隔离原型验证。
- 2026-09-20 更新：用户已明确授权实施和外部验证；原先仅拆解、暂不执行的暂停已被取代。所有测试在 D:/Project/vibe-coding-validation，论文项目不变。
- 依照已满足的依赖逐步推进 TASK-014 至 TASK-026；实时状态只维护在 tasks/task-list.yaml，不在本计划复制。
- 技术方向认可不等于批准实际项目迁移；TASK-027 是需要另行批准的发布/试点任务。
- 机器可读依赖与验收以 tasks/task-list.yaml 为准。本文件是阅读导航，不另外维护任务实时完成状态。
- 四项目标的细化验收记录为 BL-WFOPT-001..004（draft）。TASK-014 先冻结具体契约，遇到改变已批准语义的选择才提请用户决定，不把所有设计细节反复询问。

## 第一批：契约与确定性缺陷

| 任务 | 交付结果 | 前置 | 核心验收 |
|---|---|---|---|
| TASK-014 | 四项行为契约、兼容边界、脱敏最小案例 | TASK-013 | 状态联动、快照冲突、风险分流、证据适用性与旧规则兼容均有明确预期；不复制整份论文 |
| TASK-015 | Git diff 字节绑定修复 | TASK-014 | 非 UTF-8/数学字符/二进制 diff 不崩溃；Git 失败与空 diff 分开；未知编码不强行按 UTF-8 解释 |
| TASK-016 | Windows/Linux 统一解释器入口 | TASK-014 | 读取 vibe-python；无 py/python3 仍可正确调用已配置解释器；中文/空格路径安全；错误配置明确失败，不切 WSL |

这批不切换状态存储、不迁移项目。TASK-015 与 TASK-016 在规格冻结后可独立推进；默认串行，不预先分配子 Agent。

## 第二批：隔离原型与技术关口 G1

| 任务 | 交付结果 | 前置 | 核心验收 |
|---|---|---|---|
| TASK-017 | SQLite 事务最小闭环及单文档对照 | TASK-014 | begin→block→resume→finish；幂等、旧 revision 冲突、进程中断无半更新；不把哈希/编译放在写事务中 |
| TASK-018 | Git 文本快照原型 | TASK-017 | 导出/导入、双副本冲突、A→B→A 分支切换、导出失败后恢复；保留双方，不 last-write-wins |
| TASK-019 | 独立原型评估与后端决定 | TASK-017、018 | 审核正确性、交换成本、性能/读取量实测，明确 go/no-go；不能仅凭速度选数据库 |

G1 通过后才开始正式状态服务。若 SQLite 不满足验收，先记录失败与选择变化；切回单文档方向属于重新选型，不自动维护两个生产写后端。
TASK-019 由未承担原型实现的 reviewer 执行；可为独立 Agent 或人工。不可用时明确报告降级，不能自己给独立复核盖章。

## 第三批：正式能力落地

| 任务 | 交付结果 | 前置 | 核心验收 |
|---|---|---|---|
| TASK-020 | 统一状态命令、旧入口适配、自动摘要 | TASK-015、016、019 | 一个 finish 事务联动 Task/Run/事件，摘要失败可补生成；重复请求不重复记录；旧入口不能继续写第二套状态 |
| TASK-021 | 前置风险分流与最小上下文 | TASK-020 | quick/standard/strict 有理由和最低验证；低风险修改无需完整生命周期；不先读取历史再分类；阻塞/安全信息不被预算截掉 |
| TASK-022 | 历史结果/当前适用性与基础指纹 | TASK-020 | raw-file/text-lf/JSON 字段有版本化策略；CRLF、缺失旧路径和旧 PDF 分别解释；保留历史结果 |
| TASK-023 | 依赖驱动失效与漏检兜底 | TASK-022 | 图注变化不误伤指标验证，渲染证据按依赖重查；手工编辑/Git checkout 可被完成门禁发现；不确定时 unknown |
| TASK-024 | 严格路径独立复核门禁 | TASK-021、023 | review 绑定主体、验收项和被审版本；同人自审不满足独立性；版本变化后失效；无 reviewer 不伪造完成 |

TASK-020 先保证事务操作闭环；证据/策略未接入前不得将测试用 finish 当作可对真实严格任务宣告完成的产品入口。
TASK-021 与 TASK-022 可在 TASK-020 接口稳定后独立推进；若修改同一状态服务或 schema，则串行整合。其余按依赖推进。
语义证据先实现可证明的文件/结构字段范围；通用 LaTeX 语义解析不属于第一轮必需交付。

## 第四批：隔离迁移、跨平台验收与授权发布

| 任务 | 交付结果 | 前置 | 核心验收 |
|---|---|---|---|
| TASK-025 | 旧记录迁移预览与可回退演练 | TASK-020、021、023、024 | 原 ID/未知字段/历史证据保留；矛盾需裁决，不按时间猜；有新写入后回滚不丢数据；仅临时副本 |
| TASK-026 | Windows/Linux 集成验收与发布评审 | TASK-025 | 实际平台测试、旧命令兼容、Hooks/Skills 指令一致、Git往返、三类真实任务对照；形成独立评审和未验证清单 |
| TASK-027 | 经用户批准的本机安装与单项目试点 | TASK-026、用户另行批准 | 明确批准目标、备份和回退路径后安装；新终端验证；用户批准项目的 dry-run 经确认才 apply；记录试用开销 |

TASK-026 可以制作临时 CODEX_HOME 安装包/配置并测试，不写真实全局配置。Hooks/Skills/安装器的源码适配由 TASK-016/020/021/023/024 随对应能力同步完成，TASK-026 只做独立集成验收，不混入实现。
未取得 Linux 环境/CI 结果时，跨平台验收只能 partial/implemented-unverified；不得以本机 Windows 通过代替。
G2：TASK-026 有独立评审与验收证据后，才向用户请求 TASK-027 的精确安装/迁移授权。即使全部测试通过，也不自动修改 D:/Project/my_lunwen。
TASK-027 的试点记录只评估已有四项目标；自动经验蒸馏、通用多主同步和批量迁移其他项目不顺带执行。

## 四项目标覆盖

| 目标 | 业务规则草案 | 主任务 | 验证链 |
|---|---|---|---|
| 恢复可信 | BL-WFOPT-001 | 014、017、020 | 018、019、025、026 |
| 记录一次 | BL-WFOPT-002 | 017、018、020 | 019、025、026 |
| 入口分流 | BL-WFOPT-003 | 021 | 024、026 |
| 证据版本适用性 | BL-WFOPT-004 | 015、022、023 | 024、025、026 |

Windows/Linux 入口由 TASK-016 提供，贯穿全部任务；不是最后才补的环境适配。

## 执行规则

1. 当前 execution_authorized=true（用户已明确要求开始）；该字段仅表达本计划授权，不伪称运行时已实现自动门禁。TASK-027 的目标级批准仍独立。
2. 开工后，候选为 pending 且 depends_on 全部 done 的任务；按依赖层和 ID 选择。TASK-014 是第一个就绪候选。
3. G1/G2 与任务验收属于额外条件；依赖完成不能绕过用户审批、独立 review 或真实环境证据。
4. 每个任务有独立测试/证据，不用“文件写出来了”代替通过；未验证实现标 implemented-unverified。
5. 原型失败需要新假设和变化量，不能无限重试；失败后不继续展开生产改造。
6. 每批先交付一个可观察闭环，不先搭全量数据库表、全部状态名或空壳模块。
7. 不给尚未原型验证的总工期作承诺。TASK-019 再根据实际结果估算后续成本并调整计划。

## 本轮交付边界

原始 TASK-013 交付仅为任务拆解。当前已进入授权实施，按依赖和验收推进；尚未经过 G1/G2 的产物不冒充生产完成，实际安装与迁移仍受独立目标级批准约束。
